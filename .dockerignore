.git
.gitignore
.venv
venv
__pycache__
*.py[cod]
*$py.class
.pytest_cache
.mypy_cache
.ruff_cache
.DS_Store
.env
.env.*
*.md
!README.md

# requirements.txt kept for local pip reference only — Docker uses uv + uv.lock
requirements.txt

# Local pipeline outputs — mounted at runtime, not baked into the image
data/*
!data/.gitkeep
